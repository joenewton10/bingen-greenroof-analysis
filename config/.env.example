# Database Configuration
# Copy this file to config/.env and update with your actual credentials
# WARNING: NEVER commit config/.env to Git!

DB_HOST=localhost
DB_PORT=5432
DB_NAME=bingen_greenroof
DB_USER=postgres
DB_PASSWORD=your_postgres_password_here

# Optional: For Streamlit Cloud deployment
# Set these in Streamlit secrets dashboard instead of in config/.env